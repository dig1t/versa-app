/*describe('index scroller', () => {
	let nextSelection
	
	if (this.state.selected + 1 >= this.state.suggestions.length) {
		nextSelection = 0
	}
	
	const currentIndex = this.state.selected
	let nextSelection
  
	context('move back', () => {
    slideIndex = this.state.selected <= 0 ? this.state.suggestions.length- 1 : this.state.slideIndex - 1
  })
  
  context('move forward', () => {
    slideIndex = this.state.selected + 1 >= this.state.suggestions.length ? 0 : this.state.slideIndex + 1
  })
  
})*/