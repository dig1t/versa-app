const post = {
	maxBodyLength: 50
}

const user = {
	maxNameLength: 20,
	maxEmailLength: 320,
	maxPasswordLength: 9999
}

const profile = {
	maxFeedPagePosts: 10
}

export default {
	post,
	user,
	profile
}